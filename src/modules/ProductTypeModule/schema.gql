type Query {
  productTypes(id: ID, name: String): [ProductType]!
  productType(id: ID!): ProductType
}

type Mutation {
  addProductType(name: String!, propertyNameIds: [ID]!): Boolean @auth
  removeProductType(id: ID!): Boolean @auth
}

type ProductType {
  id: ID!
  name: String!
}

type Product {
  type: ProductType!
}
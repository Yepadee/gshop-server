type Query {
  customers(id: String): [Customer] @auth
  customer(id: String): Customer @auth
}

type Customer {
  id: ID!
  firstName: String!
  lastName: String!
  email: String!
}

type Order {
  customer: Customer! @auth
}
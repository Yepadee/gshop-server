
type Mutation {
  addPropertyValue(propertyNameId: ID!, value: String!): Boolean @auth
  removePropertyValue(id: ID!): Boolean @auth
}

type PropertyValue {
  id: ID!
  value: String!
}

type PropertyName {
  values: [PropertyValue]
}

type Property {
  values: [PropertyValue]
}

type Stock {
  properties: [PropertyValue]
}

type AvailableProperty {
  values: [PropertyValue]
}
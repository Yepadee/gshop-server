
type PayPalOrder {
  orderId: String!
  approveUrl: String!
}

input OrderItem {
  stockId: ID!
  quantity: Int!
}

type Mutation {
  createPayPalOrder(items: [OrderItem]!): PayPalOrder #TODO: return url, cancel url
  capturePayPalOrder(orderId: String!): Boolean!
}
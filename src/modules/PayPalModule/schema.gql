
type PayPalOrder {
  orderRef: String!
  approveUrl: String!
}

type Mutation {
  createPayPalOrder(
    returnUrl: String!,
    cancelUrl: String,
    shippingAddress: AddressInput
    items: [OrderItemInput]!
  ): PayPalOrder
  capturePayPalOrder(orderRef: String!): Boolean!
}
type Query {
  getItemsByPayPalOrderId(orderId: String!): [OrderItem]
}

type OrderItem {
  id: ID!
  quantity: Int!
}

type Order {
  items: [OrderItem]!
}

type Stock {
  orders: [OrderItem]! @auth
}